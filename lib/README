Make sure that the path to this dir is included in your ENV as RUBYLIB.

All homebrew Ruby modules should reside here.

For example, in your shell profile:
    RUBYLIB='/path/to/this/dir/:/path/to/some/other/dir/';
    export RUBYLIB;
