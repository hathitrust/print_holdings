During a print holdings build, intermediate files are stored in data/builds/current/.
At the end of the build, data/builds/current/ is renamed to data/builds/${YYYY-MM-DD}.
Keep archived dirs as .tgz for at least a year, in case things need to be investigated historically.